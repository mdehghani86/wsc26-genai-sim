# Author photos

Drop the three author portraits in **this folder**, named exactly:

| File             | Author              |
|------------------|---------------------|
| `dehghani.jpg`   | Mohammad Dehghani   |
| `belsare.jpg`    | S. Belsare          |
| `sadeghi.jpg`    | N. Sadeghi          |

Notes:
- `.jpg` or `.png` both work (keep the name, e.g. `dehghani.png` → update the `<img src>` if you use png).
- **Square crops look best** — they render as circles on the About page.
- Until a photo is added, the card shows the author's initials as a fallback (nothing looks broken).
- After dropping the files, tell me and I'll commit + push so they go live.
